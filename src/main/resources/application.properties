spring.application.name=API
spring.datasource.url=jdbc:mysql://localhost/bibliotechapi
spring.datasource.username=root
spring.datasource.password=root
#
# DEV NOTE: To ensure tokens are valid across restarts and across environments during development,
# set a stable JWT secret like below (keep secrets out of version control; use env vars or secrets manager):
jwt.secret=your-very-secret-value-with-min-length

